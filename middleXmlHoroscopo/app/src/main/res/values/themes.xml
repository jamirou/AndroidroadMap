<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MiddleXmlHoroscopo" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">@color/secondary</item>
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.MiddleXmlHoroscopo" parent="Base.Theme.MiddleXmlHoroscopo" />

    <style name="TextStyledApp">
        <item name="android:textStyle">bold</item>
        <item name="android:fontFamily">@font/dancing</item>
        <item name="android:textColor">@color/secondary</item>
        <item name="android:textSize">30sp</item>
    </style>

    <style name="BodyStyledApp" parent="TextStyledApp">
        <item name="android:textStyle">normal</item>
        <item name="android:textSize">20sp</item>
    </style>
</resources>